

#include<iostream>
using namespace std;
int main()
{
    int a[10],i,n,temp;
    cout<<"enter the no. of elments u want";
    cin>>n;
    cout<<"enter the no.s";
    for(i=0;i<n;i++)
        {
            cin>>a[i];
        }
    /*for(i=0;i<n/2;i++)
        {
            temp=a[i];
            a[i]=a[n-i-1];
            a[n-i-1]=temp;
        }*/
    cout<<"reverse of the string is \n";
    for(i=n-1;i>=0;i--)
        cout<<a[i];
        return(0);
   
}
